

datastore_types = ["mysql", "gae"]
#  MySQL python binding is not installing on my Mac, so using GAE instead.
#  Will change this if I get it working in time.
selected_datastor_type = datastore_types[1]
